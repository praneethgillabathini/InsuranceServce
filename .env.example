# ----------------------------------------------------------------
# Environment-specific settings and secrets.
# This file should NOT be committed to version control.
# Create a copy named .env and fill in your secrets.
# ----------------------------------------------------------------

# Required if using the "openai" provider.
OPENAI_API_KEY="your-openai-api-key-here"

# Required if using the "gemini" provider.
GOOGLE_API_KEY="your-google-api-key-here"

# Required if using the "grok" provider.
GROK_API_KEY="your-grok-api-key-here"

# Required if using the "bedrock" provider.
AWS_ACCESS_KEY_ID="your-aws-access-key-id"
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"